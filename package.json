{
  "name": "cognitive-kb-monorepo",
  "version": "1.0.0",
  "description": "Monorepo root for AI-Powered Knowledge Base Assistant",
  "private": true,
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "postinstall": "npm run install:backend && npm run install:frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently -k -p \"[{name}]\" -n \"backend,frontend\" -c \"purple,blue\" \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
